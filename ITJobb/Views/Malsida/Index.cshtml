@model IEnumerable<ITJobb.Models.Målsida>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MålsidaNamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MålsidaUrl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MålsidaNamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MålsidaUrl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MålsidaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MålsidaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MålsidaId })
        </td>
    </tr>
}

</table>
