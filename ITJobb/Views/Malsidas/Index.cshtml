@model IEnumerable<ITJobb.Models.Malsida>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MalsidaNamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MalsidaUrl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MalsidaNamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MalsidaUrl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MalsidaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MalsidaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MalsidaId })
        </td>
    </tr>
}

</table>
