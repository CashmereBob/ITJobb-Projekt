@model IEnumerable<ITJobb.Models.Rekryterare>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FöretagsNamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RekryterareURL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MailAdress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FöretagsNamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RekryterareURL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MailAdress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RekryterareId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RekryterareId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RekryterareId })
        </td>
    </tr>
}

</table>
