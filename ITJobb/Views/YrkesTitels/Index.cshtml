@model IEnumerable<ITJobb.Models.YrkesTitel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.YrkesNamn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MedelLon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.YrkesNamn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MedelLon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YrkesTitelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.YrkesTitelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YrkesTitelId })
        </td>
    </tr>
}

</table>
